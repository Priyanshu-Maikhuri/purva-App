import 'package:flutter/material.dart';

class Orders extends StatelessWidget {
  const Orders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Here is your Orders.', style: TextStyle(fontSize: 35, color: Colors.blueGrey, fontStyle: FontStyle.italic),),);
  }
}